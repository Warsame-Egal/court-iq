# Demo assets

Add `demo.gif` here — a ~15–30 second screen capture of the running app (live scoreboard, then click into a game for box score / play-by-play). A recorded GIF is preferred over a live demo link because the unofficial NBA API often blocks cloud IPs.
