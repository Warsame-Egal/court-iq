# REST API — Spring Boot (required for production/Vercel builds)
VITE_API_BASE_URL=http://localhost:8080

# WebSockets — Spring Boot host (required for production/Vercel builds; include scheme or host:port)
VITE_WS_URL=localhost:8080
