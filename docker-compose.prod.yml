# Production backend only (GCP VM, etc.). Frontend deploys separately (e.g. Vercel).
# Usage: cp .env.example .env  # edit for production, then:
#        docker compose -f docker-compose.prod.yml up -d --build

services:
  fastapi:
    build: ./nba-api
    container_name: courtiq-data
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      NBA_API_PROXY: ${NBA_API_PROXY:-}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS}
    networks:
      - courtiq-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/api/v1/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  springboot:
    build: .
    container_name: courtiq-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      SPRING_PROFILES_ACTIVE: prod
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      SERVER_URL: ${SERVER_URL}
      # e2-micro (1 GB): cap JVM heap so FastAPI + OS are not OOM-killed
      JAVA_TOOL_OPTIONS: "-Xmx384m -Xms128m"
    depends_on:
      fastapi:
        condition: service_healthy
    networks:
      - courtiq-network
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 120s

networks:
  courtiq-network:
    driver: bridge
